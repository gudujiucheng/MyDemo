<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Theme.Default.NoActionBar" parent="@android:style/Theme.Holo.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/logo</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <!-- 版本更新弹窗样式 -->
    <style name="Dialog_UpgradeVerion" parent="android:style/Theme.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <!-- 水平进度样式 -->
    <style name="progress_horizontal">
        <item name="android:indeterminateOnly">false</item>
        <item name="android:progressDrawable">@drawable/drawable_upgrade_progress_bg
        </item><!-- progress_horizontal -->
        <item name="android:minHeight">20dp</item>
        <item name="android:maxHeight">20dp</item>
    </style>

    <!-- 自定义软键盘 -->
    <style name="Widget.KeyboardView" parent="android:Widget">
        <item name="android:keyTextSize">22sp</item>
        <item name="android:keyTextColor">#ffffff</item>
        <item name="android:keyPreviewOffset">0dip</item>
        <item name="android:keyPreviewHeight">0dip</item>
        <item name="android:labelTextSize">1sp</item>
        <item name="android:verticalCorrection">-10dip</item>
        <item name="android:shadowColor">#bb000000</item>
        <item name="android:shadowRadius">1.5</item>
    </style>

</resources>
